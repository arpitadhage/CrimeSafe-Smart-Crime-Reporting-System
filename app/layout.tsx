import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "CrimeSafe - Secure Online Crime Reporting System",
  description:
    "CrimeSafe: Professional crime reporting platform for citizens and law enforcement. Report crimes, track cases, and communicate with police officers in real-time.",
  generator: "v0.app",
  keywords: ["crimesafe", "crime reporting", "police", "law enforcement", "emergency", "safety", "security"],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        <Suspense fallback={<div>Loading...</div>}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
